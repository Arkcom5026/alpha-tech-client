import { createProductSchema } from "./createSchema";

// ✅ editSchema.js
export const editProductSchema = createProductSchema.extend({
    // ในอนาคตสามารถเพิ่ม field เช่น id, updatedAt ได้ที่นี่
  });